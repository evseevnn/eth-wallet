mutation SendTransaction(
  $address: String!
  $amount: String!
  $password: String!
) {
  sendTransaction(
    input: { address: $address, amount: $amount, password: $password }
  ) {
    hash
  }
}
